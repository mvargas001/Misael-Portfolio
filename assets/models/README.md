Put your SolidWorks / STEP / glTF files here (.SLDPRT, .SLDASM, .STEP, .glb).

See the main README ("Adding your own photos and CAD files") for how to link them
from a project in `js/data.js`, and how to export a glTF for the in-browser 3D viewer.
